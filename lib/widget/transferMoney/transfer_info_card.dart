import 'package:flutter/material.dart';

class TransferInfoCard extends StatelessWidget {
  const TransferInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
